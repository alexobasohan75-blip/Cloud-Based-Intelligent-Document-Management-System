# Project Alignment
This implementation follows the refined build: Django backend, MongoDB NoSQL database, local media storage for PythonAnywhere, Scikit-Learn-ready TF-IDF + LinearSVC classification, Sentence Transformers semantic retrieval and audit logging.
